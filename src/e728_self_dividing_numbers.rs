struct Solution;

impl Solution {
    pub fn self_dividing_numbers(left: i32, right: i32) -> Vec<i32> {
        use std::collections::{HashMap, HashSet};
        let res = Vec::new();
        let digs: HashMap<i32, HashSet<i32>> = HashMap::new();
        res
    }
}
